Final project for CSCI 608 analyzing the average transit speeds by route in California.  Data set sourced from the California Department of Transportation (Caltrans):

https://data.ca.gov/dataset/average-transit-speeds-by-route

# Build 

To build the project, create a Python virtual environment and install the required packages:

```bash
python -m venv venv
active/Scripts/Activate.ps1
pip install -r requirements.txt
```

